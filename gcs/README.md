# Google Cloud Storage (GCS)

## What is GCS?

Google Cloud Storage is an object storage service used to store files and data in Google Cloud.

## Hands-on

Created a GCS bucket for the streaming data pipeline.

Uploaded the sample `sales.csv` file to a `raw/` folder.

### GCS Structure

```text
GCS Bucket
└── raw/
    └── sales.csv